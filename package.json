{
  "name": "mnm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "npm run build && next export",
    "predeploy": "npm run export && touch out/.nojekyll",
    "deploy": "gh-pages -t -d out"
  },
  "dependencies": {
    "next": "9.1.3",
    "react": "16.11.0",
    "react-dom": "16.11.0"
  },
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "touch-cli": "0.0.1"
  }
}
